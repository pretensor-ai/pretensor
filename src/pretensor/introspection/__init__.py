"""Schema introspection: project config, DSN parsing, and compatibility shims.

Schema snapshot DTOs, PostgreSQL introspection, and ``diff_snapshots`` live in
``pretensor.connectors``. This package re-exports ``inspector.inspect``,
``snapshot`` helpers, and ``models.schema`` for backward compatibility.
"""
