"""Quickstart assets shipped with the wheel.

Holds the docker-compose file and pagila SQL fixtures consumed by
``pretensor quickstart``. Resolved at runtime via ``importlib.resources`` so
they work the same in editable installs and in pip-installed wheels.
"""
