# Connectors — schema introspection (graph layer)

## Read first

- [docs/contracts/architecture.md](../../../docs/contracts/architecture.md)

PostgreSQL connector and `inspect()` live in this package; graph product routing: [SKILL.md](../../../SKILL.md).

## Verification

Run tests from repo root:

```bash
uv sync --extra dev
uv run pytest tests/
```
